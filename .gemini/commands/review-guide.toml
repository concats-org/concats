# /review-guide — write a guided tour of a diff for the concats-app app.
# The skill text is injected below so the model needs zero extra steps to
# see it; inside the app's terminal the CONCATS_APP_* env picks the range.

description = "Write a guided review tour of a diff (concats-app); args: [base] [head]"

prompt = """
Follow the skill below exactly, especially its Hard rules. Arguments given
to this command are `[base] [head]` overrides: "{{args}}". If they are
empty, run every concats command WITHOUT --repo/--base/--head flags:
while the review app is open, bare commands target the diff its pane shows
(the app publishes its live range; the terminal env is the fallback), and
anywhere else the CLI errors and tells you what to pass — relay that and
ask; never invent a range.

!{concats skill review-guide}

If the skill text above is missing or the command failed, run
`concats skill review-guide` yourself and follow its output; if that
also fails, report the error verbatim and stop.
"""
